#include <iostream>
#include <bits/stdc++.h>
using namespace std;
/*string subtract(string d , int x){
    for(int i= d.size()-1 ,j=0; i>=0 ; i--,j++){
        int c = d[i]-'0';
        if(c>x){
            string res = d.substr(0,d.size()-1) + to_string(c-x);
            return res;
        }
        else{
            int cc = d[i-1]-'0';
            cc--;
            int t = c+10;
            string res = d.substr(0,d.size()-2) + to_string(cc) + to_string(t-x);
            return res;
        }

    }
}*/
int main() {
    long long p,d;
    int x;
    cin >> p >> x;
    /*for(int i= p.size()-1 ,j=0; i>=0 ; i--,j++){
        if(p[i] != '9'){
            int c = p[i]-'0';

        }
    }*/
    return 0;
}
